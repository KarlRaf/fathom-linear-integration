# Commands to Run After Creating GitHub Repository

After you've created the repository at https://github.com/new with the name `fathom-linear-integration`, run these commands:

```bash
git remote add origin https://github.com/karlrafidimanana/fathom-linear-integration.git
git branch -M main
git push -u origin main
```

That's it! Your code will be pushed to GitHub.

